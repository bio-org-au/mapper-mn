language: groovy #
jdk:
  - openjdk8 #
sudo: false #
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/ #
services:
  - postgresql
script:
  - gradle clean
  - gradle build
